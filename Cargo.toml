[package]
name = "tomli-rust"
version = "0.1.0"
edition = "2021"
description = "A production-quality TOML parser migrated from Python Tomli"
authors = ["Port Mortem Hackathon"]

[dependencies]
# 100% dependency-free as per architectural constraints

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "parse"
harness = false
